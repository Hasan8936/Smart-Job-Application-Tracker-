# Smart Job Tracker — Frontend

Vite + React frontend scaffold for Smart Job Tracker.

Setup

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend API at `http://localhost:8080/api` by default. Set `VITE_API_BASE` to change it.
